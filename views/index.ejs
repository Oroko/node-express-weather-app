<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Weather App</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container">
    <header>
      <div class="compass-icon"><img src="/icons/compass2.svg" alt=""></div>
    </header>
    <div class="weather-title">
      <h1>Hali Ya Anga</h1>
      <p>View the current weather for your city</p>
    </div>
    <form action="/" method="POST">
      <input type="text" name="city" class="city-input" placeholder="Enter a City">
    </form>

    <div class="result">
      <% if(temp !== null){%>
        <h1 class="temp"><%= temp %></h1>
      <% } %>
      <div class="icon-description">
         <% if(icon !== null){%>
           <img class="icon" src="http://openweathermap.org/img/wn/<%= icon %>.png" alt="">
           
        <% } %>
         <% if(description !== null){%>
          <h3><%= description %></h3>
        <% } %>
       
      </div>
       <% if(city !== null){%>
           <p class="city"><%= city %></p>
        <% } %>
     
    </div>
  </div>
</body>
</html>
